// Copied into the CI-generated project by the "Copy your custom app code"
// workflow step, same as metro.config.js - see that file's comment.
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
